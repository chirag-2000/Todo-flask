templates canot be used directly in url as static files but by use of render_template class from flaskyou can add templates in the flask file and run them directly

 <!-- jinja2 Python template engine used to create HTML  Jinja2's syntax is inspired by Django's built-in template engine Jinja2 is a commonly-used templating engine for web frameworks such as Flask  .Template Engines: Template engines take in tokenized strings and produce rendered strings with values in place of the tokens as output.-->